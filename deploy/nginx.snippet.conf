# SPA fallback: serve index.html for routes like /dashboard so reload and direct URLs work.
# Include this inside your server { } block, or add to an existing location / { }.
location / {
  try_files $uri $uri/ /index.html;
}
