# Test deployment (vite build --mode test)
# Points to the dev/staging API — same as local dev default in .env
VITE_DRAHT_API_URL=https://dev.draht.hands-on-technology.org/custom/handson/api_proxy.php
